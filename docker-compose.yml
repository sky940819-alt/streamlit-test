version: "3.8"

services:
  streamlit-app:
    build: .
    container_name: streamlit-test
    ports:
      - "8501:8501"
    restart: unless-stopped
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
